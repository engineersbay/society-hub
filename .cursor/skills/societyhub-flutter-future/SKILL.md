---
name: societyhub-flutter-future
description: >-
  SocietyHub Flutter native apps — FUTURE ONLY. Use only when explicitly asked
  to work on post-MVP mobile apps. For MVP UI, use the React responsive web skill
  instead.
---

# SocietyHub — Flutter (future)

## Rules

- **Out of Phase 1.** Do not create a Flutter app unless the user explicitly requests future-scope mobile work and Spec/PRD are updated.
- Placeholders only: [`apps/mobile/`](../../../apps/mobile/README.md), [`android/`](../../../apps/mobile/android/README.md), [`ios/`](../../../apps/mobile/ios/README.md).
- Phase 1 client is responsive React web ([societyhub-react-vite-tailwind](../societyhub-react-vite-tailwind/SKILL.md)).
- When Flutter is in scope later, scaffold into `apps/mobile/` (or replace placeholders) and reuse the same API and `packages/sdk` contracts; do not fork business rules.
