---
name: societyhub-flutter-future
description: >-
  SocietyHub Flutter native apps — FUTURE ONLY. Use only when explicitly asked
  to work on post-MVP mobile apps. For MVP UI, use the React responsive web skill
  instead.
---

# SocietyHub — Flutter (future)

## Rules

- **Out of MVP.** Do not create Flutter apps unless the user explicitly requests future-scope work and Spec/PRD are updated.
- MVP client is responsive React web ([societyhub-react-vite-tailwind](../societyhub-react-vite-tailwind/SKILL.md)).
- When Flutter is in scope later, reuse the same API and `packages/sdk` contracts; do not fork business rules.
